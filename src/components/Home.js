import React, { Component } from 'react'

class Home extends Component {
    render () {
        return (
            <div className="container">
                <p>This is the Home page.</p>
                <p>Content for the Home page lives here.</p>
                <p>There’s no major change to how you can use code splitting in Create React App 2.0 as most of the changes were made under the hood (by upgrading to webpack 4). It’s the same method as it was in the previous version.</p>
            </div>
        )
    }
}

export default Home